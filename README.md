# 🚀 Next.js + NodeJS + MySQL CRUD System

- ✅ **Create**: เพิ่มสมาชิกใหม่พร้อมอัปโหลดรูปภาพ
- ✅ **Read**: ดึงข้อมูลจาก MySQL มาแสดงผลในรูปแบบตาราง
- ✅ **Update**: แก้ไขชื่อ-อีเมล และเปลี่ยนหรือลบรูปภาพเฉพาะส่วนได้
- ✅ **Delete**: ลบข้อมูลสมาชิกพร้อม **ลบไฟล์รูปภาพในโฟลเดอร์ uploads ออกโดยอัตโนมัติ**
- ✅ **UI**: ใช้งานง่ายด้วย Bootstrap และ SweetAlert2 สำหรับแจ้งเตือน

---

## 📂 โครงสร้างโฟลเดอร์ที่สำคัญ (Project Structure)

| Path | Description |
| :--- | :--- |
| `src/app/api/users/route.js` | **Backend API**: จัดการ Logic ฐานข้อมูลและ File System (FS) |
| `src/app/page.tsx` | **Frontend**: หน้า UI หลักและการเชื่อมต่อ API |
| `public/uploads/` | **Storage**: โฟลเดอร์สำหรับเก็บไฟล์รูปภาพที่ถูกอัปโหลด |

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
* **Framework**: Next.js 14/15 (App Router)
* **Runtime**: Node.js
* **Database**: MySQL (Library: `mysql2`)
* **Styling**: Bootstrap 5
* **Alert System**: SweetAlert2

---

## 🛠️ ขั้นตอนการสร้างโปรเจกต์ (Setup)

หากต้องการสร้างโปรเจกต์ใหม่ตั้งแต่ต้น ให้ใช้คำสั่งดังนี้:

### 1. สร้างโปรเจกต์ Next.js
```bash
cd crud-nextjs-nodejs-mysql
```
เลือกค่าดังนี้: TypeScript: No | ESLint: Yes | Tailwind CSS: No | src/: Yes | App Router: Yes

### 2. ติดตั้ง Library ที่จำเป็น
```bash
npm install bootstrap mysql2 sweetalert2
```

### 3. Clone โปรเจกต์
```bash
git clone [https://github.com/663170010318/crud-nextjs-nodejs-mysql.git](https://github.com/663170010318/crud-nextjs-nodejs-mysql.git)
cd crud-nextjs-nodejs-mysql
```

### 4. ติดตั้ง SQL ที่ให้ src\sql

### 5. รันโปรเจกต์
```bash
npm run dev
```
เข้าใช้งานได้ที่: http://localhost:3000